language: node_js
node_js:
  - 'node'
before_install:
  - cd frontend
after_success:
  - npm install yarn
  - npm run build
  - npm install -g firebase-tools
  - firebase deploy --token $FIREBASE_TOKEN --non-interactive
